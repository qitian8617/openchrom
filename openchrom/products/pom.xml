<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>net.openchrom</groupId>
    <artifactId>master</artifactId>
    <version>1.6.32-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <artifactId>products</artifactId>
  <packaging>pom</packaging>
  <name>OpenChrom Products</name>
  <!--
    Explicit list so the Baijiu workstation is wired the same way as community.
    Tycho pomless would also discover these directories (folder name "products"
    is an aggregator name), but a checked-in pom cannot drop a sibling product.
  -->
  <modules>
    <module>net.openchrom.rcp.compilation.community.product</module>
    <module>net.openchrom.rcp.compilation.baijiu.product</module>
  </modules>
</project>

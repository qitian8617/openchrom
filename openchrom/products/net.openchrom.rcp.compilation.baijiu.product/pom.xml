<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>net.openchrom</groupId>
    <artifactId>master</artifactId>
    <version>1.6.32-SNAPSHOT</version>
    <relativePath>../..</relativePath>
  </parent>
  <artifactId>openchrom.compilation.baijiu</artifactId>
  <packaging>eclipse-repository</packaging>
  <name>Baijiu FID Workstation Product</name>
  <build>
    <plugins>
      <!--
        This module redeclares the plugin, and Maven then uses this
        configuration instead of merging it with the parent's. Repeat the
        target artifact and the JustJ build-time property from openchrom/pom.xml.
        Environments are only win32/win32/x86_64 so materialize-products does
        not assemble the other five platforms.
      -->
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>target-platform-configuration</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <target>
            <artifact>
              <groupId>${tycho.target.groupId}</groupId>
              <artifactId>${tycho.target.artifactId}</artifactId>
              <version>${tycho.target.version}</version>
            </artifact>
          </target>
          <environments>
            <environment>
              <os>win32</os>
              <ws>win32</ws>
              <arch>x86_64</arch>
            </environment>
          </environments>
          <dependency-resolution>
            <profileProperties>
              <org.eclipse.justj.buildtime>true</org.eclipse.justj.buildtime>
            </profileProperties>
          </dependency-resolution>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-p2-director-plugin</artifactId>
        <configuration>
          <products>
            <product>
              <id>net.openchrom.rcp.compilation.baijiu.product.id</id>
              <rootFolders>
                <macosx>BaijiuFID.app</macosx>
              </rootFolders>
            </product>
          </products>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
